FROM node:20-alpine

RUN apk add --no-cache bash curl

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci

COPY prisma ./prisma
RUN npx prisma generate

COPY . .

CMD ["npx", "tsx", "src/workers/scheduler.ts"]
